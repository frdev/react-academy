{
  "name": "academy",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @academy/web dev",
    "build": "pnpm --filter @academy/web build",
    "test": "pnpm -r test",
    "test:run": "pnpm -r test:run",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "pnpm": {
    "overrides": {
      "@rollup/pluginutils": "5.3.0",
      "electron-to-chromium": "1.5.363"
    }
  }
}
